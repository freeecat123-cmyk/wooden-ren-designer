/**
 * @deprecated 這個檔案的內容已合併到 `lib/templates/_helpers.ts`。
 * 此檔留下空 re-export 供舊 import 路徑相容。新 code 請從 `_helpers` 引入。
 */
export {
  computeSplayGeometry,
  legShapeLabel,
  LEG_SHAPE_LABEL,
} from "../_helpers";
